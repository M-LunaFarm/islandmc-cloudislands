core-api:
  auth-token: "${file:plugins/CloudIslands/secrets/core-token}"
  admin-token: "${file:plugins/CloudIslands/secrets/admin-token}"
forwarding:
  required: false
  secret: ""
route-session:
  enforce: false
  required: false
trusted-proxies: []
proxy-source-allowlist:
  required: false
admin-command-dispatch:
  enabled: false
